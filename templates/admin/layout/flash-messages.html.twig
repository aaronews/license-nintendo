{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} text-center flash-message">
            {{ message|trans }}
        </div>
    {% endfor %}
{% endfor %}