{% extends 'mails/layout.html.twig' %}

{% block mailBody %}
    {{'mails.reset_password.body'|trans|replace({
        '%RESET_PASSWORD_URL%' : url('reset_password', {token: resetToken.token}),
        '%EXPIRATION_DELAY%' : tokenLifetime|date('g')
    })|raw}}
{% endblock %}