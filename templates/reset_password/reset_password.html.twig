{% extends 'layout/layout.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('signup') }}
{% endblock %}

{% block title %}{{'reset_password.reset_password.title'|trans}}{% endblock %}

{% block body %}
    <main class="signup d-flex align-items-center">
        <div class="container">
            <section class="row justify-content-center">
                <h1 class="text-center content-title">{{'reset_password.reset_password.title'|trans}}</h1>
                <div class="col-12">
                    {{ form_start(resetForm) }}
                        <div class="row">
                            <div class="col-12 form-group">
                                {{ form_row(resetForm.plainPassword) }}
                            </div>
                            <div class="col-12 form-group text-center">
                                <button class="btn btn-primary btn-lg">{{'form.button.validate'|trans}}</button>
                            </div>
                        </div>
                    {{ form_end(resetForm) }}
                </div>
            </section>
        </div>
    </main>
{% endblock %}